#include <iostream>
#include <math.h>
#include <stdio.h>

using namespace std;
using cap = int;
using zdog = long long int;

cap main() 
{
    zdog wow = pow(62, 60) - 1;
    printf("woow, %d \n", wow);
    zdog foo = 2;

    printf("is this gonna work, %d", wow * foo);
    return 0;
}